/**
 * A PVDatabase is a set of smart, memory resident records,
 * where each PVRecord has data composed of a top level PVStructure,
 * and a name which is the channelName for pvAccess.
 */

package org.epics.pvdatabase;
